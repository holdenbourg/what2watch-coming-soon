{
  "name": "what2watch-coming-soon",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=esm --minify --outfile=assets/main.js",
    "dev": "esbuild src/main.ts --bundle --format=esm --sourcemap --watch --outfile=assets/main.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.10",
    "@netlify/functions": "^4.2.5"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}